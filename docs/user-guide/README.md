# User Guide

Aura_script_lsns is used from either the Resonance GUI or the local CLI.

Start here:

```powershell
.\scripts\run_cli.ps1 gui resonance
.\scripts\run_cli.ps1 tasks resonance
```

The GUI focuses on market data, trade planning, automatic trade loops, city operations and battle dispatch inputs.
